# Environment Variables for Render Deployment
# Copy these to your Render environment variables section

# MongoDB Connection (Required)
MONGODB_URI=your_mongodb_atlas_connection_string_here

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration
CORS_ORIGIN=*

# Example MongoDB Atlas connection string format:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/smallcircle?retryWrites=true&w=majority
